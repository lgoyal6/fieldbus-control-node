[package]
name = "fieldbus-core"
description = "no_std control-node core: J1939-style CAN framing, PI control, watchdog, deadline telemetry"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]

[dev-dependencies]
# Test-only, so it never reaches the thumbv7em build: the drift guard in
# core_tests.rs reads manifest/frozen.json and compares it against the
# constants in this crate.
serde_json = "1"

[lints.rust]
unsafe_code = "forbid"
