# fieldbus-control-node

A periodic control node with J1939-style CAN framing, a sensor-staleness
watchdog, deadline telemetry, a deterministic bus simulator, and a Linux
SocketCAN backend.

Status: stub. See `manifest/frozen.json` for the frozen experiment design and
`results/completion.json` for measured numbers once the completion gate has
been run.
